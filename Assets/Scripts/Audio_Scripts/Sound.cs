using UnityEngine.Audio;
using UnityEngine;

public class Sound 
{
    
    public AudioClip curClip;

    public float vol; 
    public float pitch; 

    // loadInBackground: 
    /*
        Corresponding to the "Load In Background" flag in the inspector, when this flag is set, 
        the loading will happen delayed without blocking the main thread. 
    */

    // loadState: Returns the current load state of the audio data associated with an AudioClip.



}